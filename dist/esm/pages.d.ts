import { JSX } from 'react';
import { NextTopLoaderProps } from './app';
export declare const PageProgressBar: ({ color: propColor, height: propHeight, showSpinner, crawl, crawlSpeed, initialPosition, easing, speed, shadow, delay, template, zIndex, showAtBottom, }: NextTopLoaderProps) => JSX.Element;
